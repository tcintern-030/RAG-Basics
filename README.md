# RAG-Basics
This project demonstrates the basics of text preprocessing for Retrieval-Augmented Generation (RAG) using LangChain. It loads a sample text document, splits it into chunks of different sizes, compares the resulting chunks, and generates embeddings for each chunk using Google's Gemini Embedding model.
